[step]
model = "Opus"
max_turns = 10000
max_retries = 5
timeout_seconds = 600

[task]
enhance_model = "Sonnet"

[session]
# Maximum number of tasks to execute in parallel.
# Default is 1 (sequential). Set to 2-3 for parallel DAG execution.
# Can also be overridden per-run with: rloop run --parallel N
# max_parallel = 1
