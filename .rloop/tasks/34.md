---
id: 34
title: "Reduce complexity in strip_markdown (cyclomatic: 18)"
model: Opus
completed: true
---

src/render/mod.rs:125 — strip_markdown has cyclomatic complexity 18, the highest in the codebase.

Break this function into smaller, focused helpers. The function likely handles multiple markdown constructs (headings, links, code blocks, emphasis, etc.) — each construct type could be its own small function. This makes the logic easier to test in isolation and reduces cognitive load.

Target: cyclomatic complexity under 10. All existing snapshot tests must still pass.