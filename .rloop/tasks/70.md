---
id: 70
title: "task 70: Extract shared rendering dispatch helper to reduce lib.rs complexity"
model: Opus
depends_on: [68]
verification: "cargo test && cargo clippy --all-targets"
completed: true
---

Extract duplicated rendering dispatch logic into shared helpers to cut cognitive complexity.

## Changes

1. Extract a helper for the recursive rendering dispatch shared between `handle_output` (lines 930-945) and `handle_workspace` (lines 1072-1087):
   ```rust
   fn render_recursive(
       w: &mut impl Write,
       index: &DocIndex,
       idx: usize,
       cli: &Cli,
   ) -> Result<()> { ... }
   ```
2. Replace the duplicated blocks in `handle_output` and `handle_workspace` with calls to this helper
3. Similarly extract the `--recursive --source` rendering pattern shared between `handle_recursive_source` and `handle_workspace` into a helper like `render_recursive_source()`
4. After extraction, check if `#[allow(clippy::too_many_arguments)]` on `handle_recursive_source` can be removed

## Context
See docs/plans/2026-03-05-code-review-fixes.md for full plan.