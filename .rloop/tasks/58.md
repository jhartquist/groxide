---
id: 58
title: "Remove local crate caching (always rebuild)"
model: Opus
verification: "cargo fmt -- --check && cargo clippy --all-targets && cargo test"
completed: false
---

## Remove caching for local/current crates

**Plan reference:** docs/plans/2026-03-05-cli-redesign-implementation.md — Phase 3, Task 10

Local crates change with every edit. Cargo incremental compilation makes repeated rustdoc builds fast. Remove local crate caching to eliminate mtime tracking complexity.

### What to do

1. **Change `cache_path` in `src/cache.rs:46-57`:**
   Return `None` for `CrateSource::CurrentCrate`:
   ```rust
   CrateSource::CurrentCrate { .. } => None,
   ```

2. **Remove `CacheMetadata::CurrentCrate` variant** from `src/cache.rs:37`:
   - Remove the variant from the enum
   - Remove the `CurrentCrate` arm from `create_header` (~line 176-182)
   - Remove the `CurrentCrate` arm from `is_cache_valid` (~line 216-226)

3. **Remove mtime functions:**
   - Delete `get_newest_source_mtime` (~line 258-265)
   - Delete `walk_for_newest_mtime` (~line 268-292)
   - Make `get_newest_source_mtime` pub(crate) removal — check if anything else uses it

4. **Update tests in `src/cache.rs`:**
   - Remove `round_trip_save_load_preserves_index` (uses CurrentCrate)
   - Remove `stale_cache_detected_when_source_newer` (mtime test)
   - Remove `cache_path_includes_version_for_current_crate`
   - Remove `cache_path_current_crate_under_target`
   - Remove `load_cached_returns_none_for_source_type_mismatch` (uses CurrentCrate)
   - Remove `get_newest_source_mtime_*` tests
   - Remove `atomic_write_no_temp_file_left_behind` if it uses CurrentCrate source
   - Keep all Dependency, External, and Stdlib tests
   - Add a test: `cache_path_returns_none_for_current_crate`

5. **Verify no other code depends on these functions** — grep for `get_newest_source_mtime`.

### Pre-commit checks
```bash
cargo fmt -- --check
cargo clippy --all-targets
cargo test
```