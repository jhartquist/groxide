---
id: 27
title: "Remove dead code (12 items with allow(dead_code))"
model: Opus
completed: false
---

Delete all 12 dead code items identified in CLEANUP.md section 1:

- src/resolve.rs: current_package_name(), current_package_version(), target_directory(), all_package_names()
- src/render/ambiguous.rs: render_readme(), render_readme_not_found(), render_readme_stdlib_unavailable()
- src/render/json.rs: render_json_list(), collect_json_list_items()
- src/stdlib.rs: stdlib_cache_path()
- src/types.rs: grouped_items_total()
- src/types.rs: DocIndex::len() (only used in tests — move to #[cfg(test)] or delete)

Remove the #[allow(dead_code)] annotations and the functions themselves. Run cargo check and cargo test to verify nothing breaks.