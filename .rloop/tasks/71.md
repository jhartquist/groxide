---
id: 71
title: "task 71: Fix Vec::insert(0) performance in index_builder.rs"
model: Opus
verification: "rm -rf target/groxide/ && cargo test && cargo clippy --all-targets"
completed: true
---

Fix O(n^2) behavior in `reconstruct_path()` which calls `segments.insert(0, ...)` in a loop (up to 20 iterations).

## Changes

1. In `reconstruct_path()` (line 260-290): Change `segments.insert(0, parent_name.clone())` to `segments.push(parent_name.clone())`
2. After the loop (before `segments.join("::")`), add `segments.reverse()`

## Important
Delete `target/groxide/` cache before testing per CLAUDE.md since index_builder.rs was changed.

## Context
See docs/plans/2026-03-05-code-review-fixes.md for full plan.