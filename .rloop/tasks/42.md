---
id: 42
title: "Fix GitHub URLs: groxide/groxide -> jhartquist/groxide"
model: Opus
verification: "grep -r 'groxide/groxide' README.md Cargo.toml && exit 1 || echo 'No bad URLs found'"
completed: false
---

The GitHub repo lives at `jhartquist/groxide` but several URLs incorrectly reference `groxide/groxide`, which will 404.

Fix these locations:
1. `README.md:42` — change `git clone https://github.com/groxide/groxide.git` to `git clone https://github.com/jhartquist/groxide.git`
2. `Cargo.toml:8` — change `repository = "https://github.com/groxide/groxide"` to `repository = "https://github.com/jhartquist/groxide"`
3. `Cargo.toml:9` — change `homepage = "https://github.com/groxide/groxide"` to `homepage = "https://github.com/jhartquist/groxide"`

Verify no other files reference the wrong org. The CI badges in README.md already correctly use `jhartquist/groxide`.