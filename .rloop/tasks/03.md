---
id: 3
title: "Test fixture crate"
model: Opus
depends_on: [2]
verification: "mise run check"
completed: false
---

## Spec
- 03 §1 (rustdoc generation command)

## Creates
- `test-fixtures/groxide_test_api/` — a Rust library exercising all item kinds
  - structs (with generics), enums (with variants), traits (required + provided methods)
  - functions, consts, type aliases, macros, statics, unions
  - re-exports (`pub use`), glob re-exports (`pub use module::*`)
  - nested modules (3+ levels), doc comments with examples
  - private items, feature-gated items (`#[cfg(feature = "...")]`)
- Pre-generate rustdoc JSON:
  `cargo +nightly rustdoc --output-format json -Z unstable-options`
  Commit as `test-fixtures/groxide_test_api.json`

## Acceptance
- `cargo check` in fixture dir passes
- JSON file exists and is valid
- JSON contains all expected item kinds

## Commit
`task 4: test fixture crate`
