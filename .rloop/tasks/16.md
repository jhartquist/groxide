---
id: 16
title: "Orchestration — lib.rs run()"
model: Opus
depends_on: [7, 8, 10, 13, 15]
verification: "mise run check"
completed: true
---

## Spec
- 05 §3 (orchestration flow)

## Creates/Updates
- `src/lib.rs` — full `run()` implementation wiring all modules together

Implements:
- Complete flow: CLI → discover → resolve → load/build index → query → render → output
- Auto-fetch fallback when crate not in deps
- Single-segment reinterpretation
- Multi-crate search fallback
- --search dispatch
- --readme dispatch
- --source dispatch
- All stderr progress messages
- Exit code handling

## Tests (integration tests using fixture crate, TDD)
- Basic query returns expected output
- Unknown item returns exit code 1
- Crate root query (no item segments)
- Search query returns results
- List mode output
- JSON mode output

## Acceptance
- End-to-end flow works for the fixture crate
- All output modes produce correct results

## Commit
`task 17: orchestration — lib.rs run()`
