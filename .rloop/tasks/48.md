---
id: 48
title: "Update CHANGELOG date and add documentation fixes entry"
model: Opus
depends_on: [40, 41, 42, 43, 44, 45, 46, 47]
verification: "grep -qE 'Unreleased|Fixed|Changed' CHANGELOG.md && echo 'Updated'"
completed: true
---

The CHANGELOG.md has `[0.1.0] - 2025-01-01` which is a placeholder date.

1. Change the date to `Unreleased` — the actual release date will be set when we publish to crates.io. Format: `## [0.1.0] - Unreleased`

2. Add a new subsection under 0.1.0 called '### Fixed' (after the existing '### Added' section) with:
   - Fixed MSRV declaration (1.75 -> 1.85) to match actual dependency requirements.
   - Fixed repository URLs in Cargo.toml and README.
   - Fixed outdated clippy command references in documentation.

3. Add a new subsection '### Changed' with:
   - Removed mise task runner dependency; pre-commit checks are now documented as plain cargo commands.
   - Added project context documentation to README (in-project vs outside behavior).
   - Added security considerations section to README.