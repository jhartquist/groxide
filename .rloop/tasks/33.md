---
id: 33
title: "Deduplicate trim_trailing_newlines (4 copies)"
model: Opus
completed: false
---

CLEANUP.md section 2, item 1: Extract shared trim_trailing_newlines into render/mod.rs.

Current copies:
- src/render/text.rs — trims "\n\n" (keeps one newline)
- src/render/ambiguous.rs — trims "\n\n" (keeps one newline)
- src/render/list.rs — trims "\n\n" (keeps one newline)
- src/render/json.rs — trims '\n' (strips all — intentionally different)

Extract the common version (trims "\n\n") into render/mod.rs as a pub(crate) helper. Keep json.rs variant separate or parameterize. Delete the 3 private copies. Run snapshot tests with INSTA_UPDATE=always if output changes.