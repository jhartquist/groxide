---
id: 47
title: "Remove mise.toml and update all references"
model: Opus
verification: "test ! -f mise.toml && echo 'mise.toml removed'"
completed: false
---

mise adds a niche dependency for wrapping three cargo commands. Remove it in favor of documenting the commands directly.

1. Delete `mise.toml`
2. Update `CONTRIBUTING.md:7` — remove the mise prerequisite line ('- **mise** (optional): ...')
3. Update `CONTRIBUTING.md:69-70` — remove the 'mise run check' lines and just show the three cargo commands as the primary approach (not a fallback)
4. Update `CLAUDE.md:51` — change 'run `mise run check`' to 'run the pre-commit checks' and list the three commands. Remove the '(or all three manually)' parenthetical since there is no mise anymore.
5. Update `Cargo.toml:12` — remove 'mise.toml' from the exclude list (the file won't exist anymore)
6. Search for any other references to 'mise' in the project and update them.

The three commands to document as the pre-commit check are:
  cargo fmt -- --check
  cargo clippy --all-targets
  cargo test