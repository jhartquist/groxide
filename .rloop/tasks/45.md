---
id: 45
title: "Update clippy command references in docs"
model: Opus
verification: "mise run check"
completed: false
---

Pedantic clippy lints are now configured via Cargo.toml [lints] section, so the bare `cargo clippy --all-targets` command is sufficient. But several docs still reference the old CLI flags.

Update these locations to use the simpler command:
1. `CONTRIBUTING.md:74` — change `cargo clippy --all-targets -- -W clippy::pedantic -D warnings` to `cargo clippy --all-targets`
2. `CONTRIBUTING.md:87` — update the explanation to say pedantic clippy is configured in Cargo.toml [lints], not via CLI flags
3. `docs/IMPLEMENTATION.md:11` — update the clippy command
4. `docs/IMPLEMENTATION.md:569` — update the clippy command  
5. `docs/spec/05-cli-and-orchestration.md:1006` — update the clippy command

Search for any other occurrences of '-W clippy::pedantic' or '-D warnings' in the project and update them too.